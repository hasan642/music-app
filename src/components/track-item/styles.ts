/**
 * name: track-item/styles.ts
 * desc: This file contains a track-item component styles.
 */

import { StyleSheet } from 'react-native';
import { COLOR } from 'theme';

const styles = StyleSheet.create({
    titleStyle: {
        color: COLOR.DARK,
        fontWeight: 'bold'
    }
});

/**
 * export as default.
 */
export default styles;